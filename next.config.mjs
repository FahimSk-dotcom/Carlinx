/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export', // Enables static export
  basePath: '/Carlinx', // Replace with your GitHub repository name
};

export default nextConfig;
